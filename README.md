
  # LoggIT

  This is a code bundle for LoggIT. The original project is available at https://www.figma.com/design/eSRk8FtudWkSLjGrCeyCx4/LoggIT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  